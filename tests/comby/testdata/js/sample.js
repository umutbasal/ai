// Test file for JavaScript refactoring
function oldFunc(a, b) {
  return a + b;
}

oldFunc(1, 2);
oldFunc(3, 4);

fetch("https://api.example.com");
fetch("https://api2.example.com");

const old_variable_name = 42;
const another_old_name = "test";

obj.oldMethod(1, 2);
obj.oldMethod(3, 4);

oldAPI.call(arg1, arg2);
oldAPI.call(arg3);

data.oldField = 10;
result.oldField = 20;

log("ERROR", "Something failed");
log("INFO", "Everything ok");
log("DEBUG", "Test message");
