#include <stdio.h>

void oldFunc(int args) {
    printf("test");
}

int main() {
    oldFunc(42);
    
    if (x == x) {
        printf("duplicate");
    }
    
    if (a == b) {
        printf("different");
    }
    
    return 0;
}
